import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cap | AISH",
};

export default function CapPage() {
  return (
    <div>
      {/* Your Cap page content */}
    </div>
  );
} 